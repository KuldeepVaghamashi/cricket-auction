# Required
MONGODB_URI=mongodb+srv://user:pass@cluster.example/cricket_auction

# Required in production (min 32 random characters)
JWT_SECRET=

# Optional — full URLs for viewer links (admin copy link)
# NEXT_PUBLIC_VIEWER_BASE_URL=https://your-domain.com
